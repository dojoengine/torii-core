# Uncomment the block below and update the RPC URL to use the JSON-RPC fetcher.
# WIP, may not work with real data yet.
#[fetchers]
#type = "jsonrpc"
#rpc_url = "http://127.0.0.1:5050"
#page_size = 512
#max_pages = 32

[decoders.introspect]
contracts = ["0x777"]

[decoders.erc20]
contracts = ["0x42"]

[decoders.erc721]
contracts = ["0x99"]

[sinks.log]
type = "log"
label = "log-sink"

[sinks.sqlite]
type = "sqlite"
label = "sqlite"
database_url = "sqlite://torii.sqlite"
